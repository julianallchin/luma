Create a 64-second lighting demonstration for the existing venue and its synthetic 120 BPM test track. The audio is intentionally a simple pulse; no music classification or stem analysis is needed.

Use only the venue's group names when targeting lights. Discover the rig visually: isolate the back movers, left wash and right wash in headless renders, with everything else dark. Verify those selections match their names.

Author four clear sections: a restrained warm opening (0–16s), alternating left/right washes (16–32s), a brighter moving-light section (32–48s), then a contrasting full-rig finale (48–64s). Include a deliberate overlap of two layers and choose a blend mode appropriate to the effect.

Stage a proposed effect in the private draft, then preview just that clip on its group with everything else dark. Preview the full draft with the rest of the score at 8, 24, 40 and 56 seconds, including the overlap. Inspect the actual 3D images, correct bad targeting or compositing, validate, then apply. All previews must remain headless. Explain any unavailable preview operation rather than substituting a saved-score render and calling it a draft preview.

Finish in at most 150 words with clip/section counts, targeted group names, delivered preview timestamps, and friction notes about the tool trajectory. If any requirement remains unmet, lead with that incomplete status. Do not claim an image was inspected unless it reached you.
