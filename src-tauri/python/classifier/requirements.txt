# Python dependencies for the joint bar classifier worker.
#
# As of v4 the classifier slices per-bar features out of the shared MERT
# cache (.npy fp16 at 75 Hz) instead of running MERT itself. The
# `BarWindowClassifier` head from the bundled `bar_window_classifier.pt`
# stays in-process; everything else (transformers, librosa, audio loading)
# moved to the `mert` preprocessor.
#
# torch / torchaudio / torchvision are NOT pinned here — python_env.rs
# strips them at write time so the cu128 GPU wheels installed in step 1
# aren't overwritten by CPU PyPI wheels.
numpy>=1.24.0
