Execute Python in a namespace that persists for this agent thread. Current Luma state lives under `luma` and is refreshed before every call; the variables, functions and imports you create persist across calls. numpy, scipy, librosa and matplotlib are available. You get back stdout, stderr, the last expression's value, a traceback when it fails, and any matplotlib figures as images you can actually see. Write normal cell-shaped Python — no wrapper function, no `return`.

For every call, describe the cell's purpose before its code. Use a short noun phrase that completes “Running …”, for example “section energy analysis.” Do not restate the code.

Orientation:
- `luma.catalog()` gives a compact overview. Drill into a relevant path, such as `luma.catalog("venue")` or `luma.catalog("features.drum_onsets")`; use `depth=` to see more of that branch. Request `depth=None` only when you need its full inventory.
- In a graph score, `edit = luma.track.edit()` captures the document and revision. Create a one-node effect with `graph = edit.graph(node="chase")`, then `edit.add_clip(graph, beats=(32, 48), selection="front_wash", inputs={"width": .4})`. Use `bars=(1, 5)` or `seconds=(start, end)` when appropriate; all ranges are half-open. Beats are zero-based from the musical origin, bars one-based. `edit.update_clip(clip, inputs={...})` changes overrides; a `None` override restores the graph default. `edit.remove_clip(clip)` deletes it. `subset=` accepts a fraction, head count or `"all"`.
- Compose custom graphs with `graph = edit.graph()`, `node = graph.node(definition_id, **inputs)`, connections through `node.output(port)`, `graph.expose(node, input)` for clip controls, and `graph.output(final.output(port))` for the result. Node definitions are shared by clips; `edit.make_independent(clip)` copies local dependencies. Built-ins are read-only. Discover with `luma.track.nodes(search)` and `luma.track.definition(id)`; follow definition references to inspect subgraphs. `edit.source()` is the canonical score.luma JSON; `edit.replace_source(source)` stages it. Native editing, Python and model-authored JSON use one validator.
- Verify with `edit.diff()`, `edit.check()` and `view = edit.window(...)`. `view.timeline()` plots clips; `view.output.heatmap()` renders the actual candidate. Inspect a proposed clip in 3D with `luma.venue.render(edit=edit, only=clip, t=...)`, then omit `only` to inspect the full draft and its blends at representative timestamps. `edit.apply()` commits and advances `luma.track`. In a child workspace it advances only the detached score, which a supervisor later merges. Opening a new edit after a conflict captures the current score.
- If the bound score still has `luma.patterns` rather than `luma.nodes`, it awaits manual migration: its legacy `edit.add_clip(pattern_id, ..., args={...})` uses the published pattern schemas. Do not pass old pattern IDs as new graph definitions.
- `luma.audio` is audio signal (mix, stems). `luma.features` is what was derived from audio (beats, downbeats, drum onsets, bar classifications, chords, waveform bands). Neither is a fallback for the other: pick the branch that matches the question.
- Tensors expose `.values` (numpy), `.shape`, `.axes`, `.times_s`, `.unit`, `.provenance`. Keyed families are dict-style: `luma.features.drum_onsets["kick"]`, `luma.audio.stems["drums"]`.
- `luma.venue` is the room. Plan coordinates are `u` (stage right), `v` (toward the audience), and height `z`, in metres; angles are degrees. Use `describe()` for a compact live overview and `nodes()` for placed geometry. `fixtures` is the patch; `fixture_library(search)` finds available models. Create named collections with `group(name, fixtures)`; distribution does not create groups. `groups()` reads the saved collections. `render(view="front", t=0.0, width=960, height=540)` returns a headless image and `.path`, without moving the user's visualizer. Views: front, audience, overhead, quarter_left, quarter_right, dj. Without `edit`, it renders the saved score. Pass `highlight="<selection expression>"` to inspect a group at full white with every other fixture dark and no score playing. The room uses its saved environment; `house=0` turns house lighting off for the preview. Highlight and draft-score previews are separate modes.
- `luma.graph.run.views` holds the latest graph run's view-node output when a graph run is in scope. All times are absolute track seconds.
- Images are collected for you, never emitted by hand: every matplotlib figure still open at the end of the cell and every `luma.venue.render(...)` made during it comes back as an image. There is no `display()` — this is a plain Python process, not IPython.
