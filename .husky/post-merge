#!/usr/bin/env sh
# Git LFS hook (merged into Husky's hook chain).
command -v git-lfs >/dev/null 2>&1 || {
  echo >&2 "\nThis repository is configured for Git LFS but 'git-lfs' was not found on your PATH.\n"
  exit 2
}
git lfs post-merge "$@"
