# Agent friction log

Where agents grumble about tooling that fights back. One line each, newest first,
`- [YYYY-MM-DD] <gripe>`. Product bugs go in the task report, not here.
- [2026-08-29] `nav.venue(name)` clicks the venue *picker* card, and that picker only exists while no venue is selected — so `nav.stage(v)` then `nav.patch(v)` in one test fails on the second call with "never saw the venue", even though the shell is already in that venue. The walk that says "go to this view" is only usable once per session, and nothing in nav.js says so.
- [2026-08-29] The gpui `Fixture` disables Python workspaces outright (`the fixture does not run Python workspaces`) and never sets an agent model, so a headless test can assert which conversation the shell points at but can never assert what a cell in it returned. An acceptance criterion phrased as "run a python cell and check the app shows the same thread" cannot be met in one harness; it splits into a `cargo test --lib` half and a headless half, and that is not written down anywhere.
- [2026-08-29] `luma_exec/tests/test_venue.py` is not in `luma_exec/tests/run_tests.py`, so it has been failing at HEAD on a stale `highlight` key while the suite everyone runs reports 31 passed. A test file nobody runs is worse than no test file.
- [2026-08-29] `render-goldens`' `scenes.json` is tab-indented, and `json.dump(..., indent=1)` — the shape the file *looks* like — rewrites all 5 400 lines. Appending one 200-line scene by script costs a `git checkout` and a round-trip identity check first.
- [2026-08-29] `Renderer::render` gives back sRGB bytes with no way to ask what the camera actually was, so the only way to find out that a POV camera sitting at the apex of its own cone integrates the whole beam and renders pure white is to render it and look. Six captures to place one fixture.
- [2026-08-29] One `cargo check --workspace` in a scratchpad worktree with no `CARGO_TARGET_DIR` filled the last 200 MB of a 1.8 TB volume, and from then on *every* Bash call failed with ENOSPC because the tool could not create its own output file — no `df`, no `rm`, no way to diagnose the thing that had just broken. Ten stale round worktrees were holding 160 GB of target dirs. A cold `gpui/target-pixel` in a fresh worktree costs hours; a second one costs the machine.
- [2026-08-29] `harness/goldens/patch-allocation.json` and `harness/goldens/venue-poses.json` serialize their top-level keys in whatever order `serde_json` happens to be built with, so `cargo test -p luma-scene` is green and `cargo test -p luma-render -p luma-scene` rewrites both files and fails — feature unification decides whether a golden matches itself. The self-healing write then leaves a 300-line diff in the tree that has nothing to do with the change under test.
- [2026-08-29] Worktrees sharing the main cargo target dir poison each other: a `cargo check` in the main checkout compiled `luma` against another worktree's in-progress `luma_scene` (a field that exists only there), failing on code that is correct at HEAD. Path deps don't get distinct fingerprints here; give worktrees their own target dir or expect ghosts.
- [2026-08-29] `until()` steps one frame per poll, and one frame does not carry a two-deep async chain back to the app — a gpui verb that spawns from inside another spawn's answer never lands, so a thousand polls over ten seconds lose to four frames in one call. Nothing in the helper says its granularity is load-bearing.
- [2026-08-29] The a11y tree reports a control's bounds and its enablement but nothing about what is painted over it, so a button sitting *under* a sheet reads as perfectly pressable and clicking it is a silent no-op. Two hours, and the only way to see it was to print two nodes' bounds side by side.
- [2026-08-29] `app.type(node, text)` appends, so a helper that opens a dialog and types into it works once and silently matches nothing the second time — the second call's query is the first one's twice over, and the failure surfaces as "no such row".
- [2026-08-29] `cargo test -p gpui-agent --test headless` does not build `luma-app`'s own test target, so a `#[cfg(test)]` module in the app crate can name a variant that no longer exists and every headless run stays green. Only `cargo clippy -p luma-app --all-targets` (or `cargo test -p luma-app`) compiles it; a matrix built out of the headless and pixel suites alone never touches it.
- [2026-08-29] `cargo test ... venue_patch -- --ignored` prints `test result: ok. 0 passed; 0 filtered out` and exits zero when the tests are not ignored — a run that executed nothing is indistinguishable from a run that passed, unless you read the count.
- [2026-08-29] `dispatch::tests::ipc_manifest_matches_the_command_table` fails on *line-number drift*: adding one line to a handler rewrites `docs/specs/ipc-manifest.json` and fails the run demanding you commit it, so any edit to `dispatch/handlers/*` costs a second test run and a file nobody read.
- [2026-08-29] Git merged two independently-written `Library::search_fixtures` methods into one impl block without a conflict marker — same name, different arity — so the only signal that a semantic conflict existed was `E0592 duplicate definitions` three minutes into `cargo check`. A clean `git status` after a merge says nothing about whether the two sides agree.
- [2026-08-29] gpui hit-testing is paint order and nothing warns you: the stage's run controls were added to the overlay before the socket beads, so every press aimed at the length box was swallowed by a bead on top of it. An hour of bisecting a drag that looked like a broken primitive.
- [2026-08-29] a control anchored to the value it edits cannot be fixed by tuning: the run's length box sat at the measurement midpoint, which is a function of the length, so dragging it slid it out from under the pointer that was dragging it. Anchor to the socket, not the span.
- [2026-08-29] `Environment::EDITOR.ambient_intensity` was restated as a literal `0.2` twice in visualizer.rs, so raising the constant changed nothing anybody could see — several minutes spent staring at an unchanged screenshot.
- [2026-08-29] `app.click` only takes a node, so "press at this point on a canvas" has to be written as `app.drag(point, {dx: 1, dy: 0})` — and a one-pixel drag is also the only way to move the pointer at all, because there is no hover/move primitive. A gesture that must *not* end in a click has to start its drag somewhere with no handler and walk into the target, which is a trick the API docs do not mention.
- [2026-08-29] `cargo test -p luma-render` in a shared checkout reports another agent's uncommitted red tests as yours, and nothing in the output distinguishes them; the only way to tell is to read their diff.
- [2026-08-29] `rustfmt --check` passes on a file `cargo fmt` would still rewrite when the path is given from the wrong directory — `rustfmt <path>` silently formats nothing rather than erroring if the path is relative to a different cwd than the one you are in.
- [2026-08-29] `cargo clippy --all-targets` from `src-tauri` prints two screens of pre-existing warnings from `prodjlink`, `stagelinq`, `python_env` and friends, so "is my diff clean" is a grep by filename rather than an exit code. Confirmed the ipc-manifest test needs three runs, not two: regenerate, still-stale, green.
- [2026-08-29] `docs/specs/ipc-manifest.{json,md}` was stale on HEAD — B3's whole `distribute` domain had never been regenerated into it — so the first agent to add a verb after that carries somebody else's 50-line diff in their commit and has to explain it.
- [2026-08-29] Four `app_pixel` budget tests were red on HEAD with `code: 1811 fixture footprint leaves its universe`, because `support::Fixture::with_rig_of(480)` seeds every mover into universe 1 at `i*8+1` and the addressing migration has refused that since it landed. Nothing named the connection: the failure surfaces inside a shared fixture, in tests whose subject is frame time.
- [2026-08-29] `search_fixtures` answered "Fixtures not initialized. Call initialize_fixtures first." and nothing in the gpui app has ever called it — so a working backend verb looked like a broken one, and the only clue was the error string.
- [2026-08-29] `app.type(node, text)` clicks the node first, so there is no way to type *over* a field's contents in one call: `click` then `key("cmd-a 1 0 0 enter")` works, but you get there by reading `pump.rs`, not the API doc.
- [2026-08-29] A filtered `cargo test --lib <name>` is not a cheap way to check one test: it relinks the whole 1000-test binary (~20-30 s in a fresh worktree), and per AGENTS.md it also rewrites `src/bindings/schema.ts` down to the types those tests touched — so every filtered run has to be paid for again with a full one before `bun run build` will typecheck.
- [2026-08-29] `ipc_manifest_matches_the_command_table` regenerates `docs/specs/ipc-manifest.{json,md}` and then fails, so adding one dispatch verb costs two full 100s `cargo test` runs before green — and the second run failed too, so it was really three. The panic says "commit the new files" without saying it already wrote them.
- [2026-08-29] Adding one `SocketType` variant fails `luma-scene`'s binding test and `dispatch`'s ipc-manifest test on the *first* run and passes on the second, because both write the new file and then assert it was unchanged. Correct, but it reads as a red suite twice per change and there is no `--accept` to say "yes, regenerate".
- [2026-08-29] `bunx vitest run --reporter=basic` does not say "that reporter is gone"; it throws a module-resolution stack out of vitest's internals, which reads like the repo is broken rather than like one flag is wrong.
- [2026-08-29] A fresh worktree needs three gitignored things hand-copied before anything builds: `node_modules` (symlink), every `src-tauri/python/*/requirements.txt`, and the bindings (regenerate, don't copy). `git ls-files --others --ignored --exclude-standard` finds only four of the six requirements files, because the nested checkouts are invisible to it — use `find`.
- [2026-08-29] `cargo test --test headless` from `gpui/` fails with "no test target named `headless` in default-run packages" and then *exits 0*, so a script believes it passed. It needs `-p gpui-agent`, which the verification matrix does not say.
- [2026-08-29] Copying `src/bindings/*.ts` from the main tree into a worktree is a trap: they can be stale vs the commit under test and `bun run build` then fails with a wall of type errors that look like the diff's fault. Regenerate via `cargo test` in src-tauri instead.
- [2026-08-29] A `git worktree` is not an isolated build: `.cargo/config.toml` points every checkout at the main tree's target dir, so a "standalone" worktree queues behind the main tree's `.cargo-lock` and shares its incremental state — the isolation is source-only, and nothing says so.
- [2026-08-29] A fresh worktree cannot build src-tauri at all: `python/consonance-ACE` is a nested checkout, so `include_str!("../python/consonance-ACE/requirements.txt")` fails and the whole lib errors before any of your code is compiled.
- [2026-08-29] `docs/specs/ipc-manifest.json` pins each handler's *line number*, so adding a sentence to a doc comment fails `ipc_manifest_matches_the_command_table` and forces every owner to commit a shared generated file.
- [2026-08-29] Sibling agents pick colliding worktree paths under the shared scratchpad (`…/scratchpad/wt`), so a critic's checkout silently acquires another owner's dirty files mid-review; no convention reserves a per-agent name.
- [2026-08-29] `bun run build` is unrunnable in any fresh worktree: `src/bindings/*.ts` is gitignored and only materialises as a side effect of `cargo test` in src-tauri — a ~40-minute prerequisite for a type check.
- [2026-08-29] `sync/tests.rs` has two pool helpers with different schemas — `test_pool` hand-rolls a subset of tables, `migrated_pool` runs the real migrations — and the `authenticate` helper only works with the hand-rolled one; on the other it dies with "no such table: auth_session", which reads as a migration bug rather than a wrong-helper bug.
- [2026-08-29] `MockRemoteClient::on_select` serves the same page on every call, so any test that drives the pull page loop trips the non-advancing-sync_seq guard instead of terminating; a cursor-shaped mock has to be built before you can test a pull at all.
- [2026-08-29] `cargo test --lib` in `src-tauri` is all-or-nothing in a shared checkout: another owner's half-written struct in an unrelated module made every test in the crate unrunnable for twenty minutes, and there is no way to compile or run just the module you touched.
- [2026-08-29] `fixtures.created_at` is second-resolution and `fixture_creation_order` tied on it with `ORDER BY ... id ASC`, so "the venue's fixtures in creation order" was uuid order for every fixture patched in the same second — the derived group tree came out in a different sibling order on every run and the test that pinned it failed intermittently with no hint that the DB was the coin flip.
- [2026-08-29] A fixture patched *after* a venue's graph conversion never gets a graph node: `venue_graph::migrate` is the only thing that creates them and it is gated on the root already existing. Nothing errors — the fixture is simply unplaceable forever, which reads exactly like "you haven't placed it yet".
- [2026-08-29] 49 of the 1680 shipped `.qxf` files do not parse at all (`duplicate field `$value``), and every consumer swallows it as "unknown fixture" — no count, no log, no test. Three percent of the catalogue has been silently unclassifiable for as long as the parser has existed.
- [2026-08-29] A test in a shared checkout cannot run `cargo test --lib` for its own module: another agent's mid-edit `src/services/patch/tests.rs` fails to compile and takes the whole test binary with it, so "is my change green" needs a second checkout every single time.
- [2026-08-29] Cold `cargo test --lib` in a fresh src-tauri worktree fails on `include_str!("../python/*/requirements.txt")` — every requirements.txt is gitignored, so a clean checkout of any commit is unbuildable until you hand-copy six files out of the working tree.
- [2026-08-29] "One list read off the schema" fixed sync::pull and silently weakened the wipe audit: dedup of two lists that answer different questions looks identical in the diff to dedup of two that answer the same one.
- [2026-08-29] Two agents in this shared tree each wrote a migration named `20260830000000_*.sql`. sqlx keys on the numeric prefix, so a duplicate version is a checksum/ordering break on every launch, and nothing warns — no test, no lint, not even a filename collision, because the suffixes differ. Anyone adding a migration in a shared checkout has to `ls migrations` and hope nobody else is mid-edit.
- [2026-08-29] `u` on a surface placement means stage *right*-positive on the venue floor and stage *left*-positive on a deck's `top` socket, because the two hosts' tangents disagree. Nothing says so; you find out when a wing you placed at `u = -4` derives as `right wing`. The design doc's own audit is about exactly this class of thing, and the fix is a note on `root_socket`/`SocketDef` saying which way a tangent points.
- [2026-08-29] `get_patched_fixtures` has no `ORDER BY`, so "the venue's fixtures in creation order" — which the group tree and the `<model> <n>` names both depend on — is rowid order by luck, stable until the first `VACUUM`. I had to add a second query just to say out loud what the first one silently almost does.
- [2026-08-29] `harness/goldens/stage-catalog.json` outlived its only reader by a whole deletion: `catalog.golden.test.ts` went away with the TS socket resolver, and the Rust test kept writing the file while `binding.rs` and the generated header both still claimed it "proves the two agree". Nothing in the repo can tell you a golden has no consumer — grepping the filename is the only check, and it finds the writer and the two stale claims but not the missing reader, so the file reads as load-bearing right up until you look for the test that opens it.
- [2026-08-29] `venue_graph::sockets()` is a process-wide `OnceLock` that takes `fixtures_root` as a parameter, so the argument is honoured exactly once per process and silently ignored by every caller after. A test that passes a temp dir gets whatever root the first test in the binary happened to pass. It works today only because `stage_render::meshes_root` falls back to the repo, which means the parameter is decorative and the coupling is invisible.
- [2026-08-29] Every `test_pool` in `src-tauri` has to open the database twice — once with `foreign_keys(false)` to run migrations, once with them on — because `20260323000000_uuid_everywhere` rebuilds tables in an order live FKs refuse. That is copied verbatim into six modules and the failure it prevents is `no such table: main.fixture_groups` from a migration that mentions neither foreign keys nor `fixture_groups`. One shared test-pool helper would have saved every one of those authors twenty minutes.
- [2026-08-29] `venue_graph::migrate` gives a fixture's node the *same id* as its `fixtures` row and also stores that id in `catalog_ref`, so a fixture is findable by two names and nothing says which one a consumer should use. I seeded a test with `node:<id>` and got a silently placement-free venue: every fixture allocated as if it were in the tray, no warning, no error, just a golden that looked plausible.
- [2026-08-29] Four agents share one working tree with no coordination primitive, so `git stash` to get a baseline test run silently reverted everyone's uncommitted work, and files I had edited and verified were overwritten under me twice. There is no way to ask "is this failure mine?" without either a stash or a second checkout, and one of those is a live grenade.
- [2026-08-29] `src/bindings/*.ts` is gitignored and generated by `cargo test`, so when another agent's concurrent Rust work makes the lib suite fail partway, `bun run build` starts failing on missing TS types that have nothing to do with either agent's change. The frontend build depends on the whole Rust test suite passing, which makes it hostage to unrelated backend churn.
- [2026-08-29] The gpui app reaches the venue only through `dispatch`, so it gets `models::venue_graph::ResolvedVenue` (the wire projection) while `stage_render::VenueGeometry` holds `luma_scene::venue::ResolvedVenue` (the solved model). Same name, same fields, no conversion either way — so "call the existing `pieces()` instead of writing a second copy" does not typecheck until you push the shared arithmetic down to a per-node function. Two types called `ResolvedVenue` in one dependency graph costs a good half hour before you believe it.
- [2026-08-29] Someone ran `git stash` in this shared checkout and silently reverted every agent's uncommitted work at once — four hours of three agents' edits, gone from the tree with no error and no warning. `git stash apply` got it back, but nothing in the repo tells a fresh agent that the working tree is shared and that the whole `stash`/`checkout --`/`reset` family is a live grenade here. That belongs in AGENTS.md, not in a friction log written after the fact.
- [2026-08-29] `luma_scene::coords` did not exist: the data<->three mirror lived in `luma_render::coords`, which is a wgpu crate, so anything GPU-free that needed the convention had to copy it. Splitting it out was ten minutes; deciding it was safe to split took an hour of reading render goldens.
- [2026-08-29] `stage_pieces` was never in `sync/registry.rs` and there is no remote table, but nothing says so where you would look — the fact is a comment in a 2026-05 migration header. Three greps and a live Postgres query to establish that a table does not sync.
- [2026-08-29] `cargo check --manifest-path src-tauri/Cargo.toml` from the repo root fails with "manifest path does not exist" if you happen to be in a subshell whose cwd moved; agent bash calls reset cwd between invocations, so half the documented commands in AGENTS.md need absolute paths that the doc does not use.
- [2026-08-29] `resources/meshes/` is at the repo root but every truss doc and golden path says `gpui/...`, so `ls resources/meshes/stage_lab` from `gpui/` lies about the reference meshes existing. Twenty minutes of `find` before the accessor dump could start.
- [2026-08-29] `AGENTS.md` still says "there is no dedicated JS test suite yet" and tells you to validate by hand; `vitest.config.ts` has run 654 tests for a while, and the stage goldens live in it. The doc that tells a new agent how to check its work was the last thing to learn how.
- [2026-08-29] The truss contract PNGs under `gpui/crates/render/goldens/contracts/` changed under me mid-task — a concurrent agent recapturing in the same checkout. Nothing warns you; `git status` just quietly grows files you did not touch, and the "commit path-scoped" rule is the only thing between that and a bogus diff.
- [2026-08-29] Nothing in the truss family rendered a face until now, so `Face::frame()` putting a block's mating plane on the *chord-centre* square went unnoticed for two commits — every corner's in-plane edge tubes bulged a chord radius through every face, and every bolted joint interpenetrated invisibly. A geometry invariant with no picture and no test is not a contract, it is a comment.
- [2026-08-29] `render-goldens` had no dry run: the only way to learn whether a change moved a golden was to overwrite all 24 of them and read `git status`, which conflates "I checked" with "I recaptured" and makes a stale scene indistinguishable from a fresh one. Two separate tasks today wanted to ask the question without answering it; `--check` now exists.
- [2026-08-29] Two agents in one checkout means `git commit -a` from either sweeps up the other's half-written files: my `scene_desc::Editor` field landed inside someone else's truss commit, which therefore does not build on its own. Path-scoped commits are the only safe form in a shared tree, and nothing enforces them.
- [2026-08-29] A shared checkout also means someone else's mid-edit crate is *your* compile error: `cargo check -p luma-render` failed on `truss.rs` and `fixture-kinematics` for twenty minutes for reasons that had nothing to do with the diff in front of me, and the forbidden `git stash` is exactly the tool that would have isolated it.
- [2026-08-29] The stage's `app.screenshot` can hand back the frame *before* the one your last click produced — the viewport is a frame behind by design — so any pixel reading has to be taken twice and believed only when it stops moving. A fixed `app.frames(n)` passes alone and fails in a full suite run.
- [2026-08-29] The frame-stats readout paints saturated red over the viewport whenever the stage dips, so "find the coloured overlay" in a screenshot finds the FPS panel too; a pixel test has to subtract the chrome's own node bounds from the picture it is measuring.
- [2026-08-29] `cargo test --lib` in src-tauri regenerates `docs/specs/ipc-manifest.*` and *then* fails the run that wrote them, so adding one dispatch command costs two full 80-second suites before anything is green.
- [2026-08-29] A new dispatch command compiles, passes the manifest test, and is still unreachable at runtime until you also add it to the `tauri::generate_handler!` list in `lib.rs`. The only signal is a `function ... is never used` clippy warning buried among pre-existing ones.
- [2026-08-29] A render-contract golden is only a gate if its id is *also* hand-copied into `CASES` in `crates/render/tests/renderer_contract_goldens.rs`; `render-contract-goldens` happily captures and tracks a PNG that nothing ever compares, and `truss-side-by-side` shipped ungated for exactly that reason.
- [2026-08-29] `git status` RM = rename already staged, so a path-scoped `git add` of an unrelated file still commits it — splitting a tree with pre-staged renames misfiles them, and fixing that needs `reset`.

- [2026-08-29] `render-contract-goldens` writes straight into the tracked `goldens/contracts/`, so the only way to *look* at a scene while iterating on geometry is to overwrite a committed golden — there is no `--out` and no dry-run render
- [2026-08-29] homebrew python 3.14's `pyexpat` is broken against the system libexpat (`Symbol not found: _XML_SetAllocTrackerActivationThreshold`), which takes `import matplotlib` down with it — any mesh/geometry inspection has to be hand-rolled numpy plus a stdlib PNG writer
- [2026-08-29] The automation node tree has no stable per-row key, so headless tests identify rows by rendered label text — which renumbers, retitles, and drifts under them (ordinals-as-identity has bitten twice).
- [2026-08-29] `cargo test --lib` in src-tauri *rewrites* `docs/specs/ipc-manifest.json` and then fails the run telling you to commit it — so a full suite in a shared checkout hands you another agent's in-flight handler in your diff, and the only way to get green is to accept their file
- [2026-08-29] migration `20260403000000_fix_updated_at_triggers.sql` documents that SQLite's `CURRENT_TIMESTAMP` (space) and RFC 3339 (`T`) compare wrong, fixes the *trigger* side, and leaves every `DEFAULT CURRENT_TIMESTAMP` alone — so `ORDER BY updated_at` still silently files edited rows above new ones and the header comment reads as if the bug were closed
- [2026-08-29] `until(what, pred)` returns the *snapshot*, not the predicate's value, so the natural `const x = until("…", () => readSomething())` hands you a Snapshot and dies three lines later on `x.slice is not a function` — the helper's own doc comment says "until a snapshot satisfies pred" and never says what it hands back
- [2026-08-29] the `+` menu only exists once the workspace panel has a tab, which no doc near `luma::NewTab` says; a test that opens it from a bare shell fails with a 30-node dump you have to read carefully to notice the control is simply absent
- [2026-08-28] the headless harness resolves `stage_gpu` to false, so the stage pane draws a plate with no hitbox and no pointer handlers at all — every visualizer input bug (orbit, dolly, gizmo) is therefore unreproducible in `--test headless` and you only find that out after writing the test, wiring the fixture and watching the leak refuse to fire
- [2026-08-28] `app.click` takes a node and nothing else while `app.drag` happily takes `{x, y}`, so pressing a *band* that is not a control (a timeline ruler) means faking a click with a zero-length drag — the asymmetry is undocumented and the error only turns up at runtime
- [2026-08-28] a one-frame flash is invisible to the harness: `app.snapshot()` settles first, and a settled command draws several frames while keeping only the last in the node registry — so a bug you can see with your eyes leaves no evidence any script can read, and pinning it meant adding frame retention (`app.painted()`) before the test could exist at all
- [2026-08-28] seeding a fixture conversation with an assistant message dies on `(code: 1811) assistant message requires a prepared authored turn` — the append seam takes any role and the rule only shows up as a raw sqlite trigger message, so "put two messages in a thread" is a five-minute detour to find out only the user half is allowed
- [2026-08-28] `cargo fmt --all` in this checkout reformats whatever every other agent has half-written in the shared tree; there is no per-crate habit documented, and you find out by watching three files you never opened change mtime
- [2026-08-28] a filtered `cargo test --lib <filter>` in src-tauri truncates `src/bindings/schema.ts` to whatever the filter touched, so the next `tsc` reports ~100 phantom "has no exported member" errors across files you never opened — it reads exactly like you broke the bindings, and the only cure is another full 106s lib run
- [2026-08-28] benchmarking the analysis pipeline on this box is unreliable without a cooldown: a straight repeat of the same 12-track batch went 178s then 253s (+42%) purely from thermal throttle, so any A/B smaller than ~40% is noise unless every run is preceded by 150s idle and the baseline is re-measured at the end
- [2026-08-28] there is no way to run the preprocessing DAG headlessly — `reprocess_track` is a Tauri command and the seven python workers each have their own bespoke argv, so measuring the pipeline means rebuilding the scheduler's layer structure by hand in a throwaway script
- [2026-08-28] restoring a file with `mv foo.rs.bak foo.rs` hands cargo back an *older* mtime than the artifact it already built, so `cargo test` silently reruns the stale binary and reports a failure the source cannot produce — twenty minutes of debugging a bound that was never broken
- [2026-08-28] `track_genres.labels_json` is compacted *per track* and every `label_index` remapped into it, so indices mean nothing across rows — the column name reads like a shared taxonomy and nothing at the SQL layer warns you before you join two tracks' genres together
- [2026-08-28] profiling the analysis pipeline has no instrument: no per-stage `duration_ms` column, no tracing span, no timing log anywhere, so the only way to get stage costs is differencing `created_at` across the seven artifact tables — which silently yields garbage (deltas of 3,000,000+ s) for any row a later version-bump backfilled, so you must first find a batch where all seven stages ran in one pass
- [2026-08-28] the harness pipe mangles a request line every few hundred calls and answers `{"id": null, "err": "malformed request JSON"}` — the shim has nobody to hand that to, so the caller's promise never settles and an hours-long script stops dead with no error, no exit, and a log that just stops
- [2026-08-28] `run_waveform_jobs` spawns one task per track with no semaphore against a 16-connection pool, so importing 65 tracks in one call makes every *other* artifact write fail with `pool timed out` — the DAG's own bound does nothing about it and the only lever a caller has is smaller batches
- [2026-08-28] a headless host arms write admission from an *expired* stored session but `session_user_id` refuses to name it, so every import dies on `Supabase access token has expired` an hour after the app last ran — and the only way through is `--fixture-principal`, whose own docs say "disposable database only", i.e. the one path that works reads as forbidden
- [2026-08-28] `startHarness({verbose})` is all-or-nothing, so following a long import means either no harness diagnostics at all or one `[event] track-import-state` line per track per phase drowning the script's own output
- [2026-08-28] `sqlite3 "file:<path>?mode=ro"` fails with error 14 on `luma.db` while the plain path opens fine — the read-only URI form is not a reliable way to inspect it; copy the db first.
- [2026-08-28] codex `exec` auto-denies every MCP tool call ("user cancelled MCP tool call") unless `--dangerously-bypass-approvals-and-sandbox`; `approval_policy="never"` and `--full-auto` do nothing for MCP on 0.150. Also 0.128→0.150 renamed the JSONL item discriminator `item_type`→`type`, silently breaking the id scraper.
- [2026-08-28] `codex exec` has no `--max-turns` or any turn/tool-call cap, so a probe run of an open-ended brief can only be bounded by shrinking the brief — the closest thing in the config surface is nothing at all
- [2026-08-28] one bad key in `~/.codex/config.toml` (`service_tier = "default"`, rejected by 0.128) makes *every* `codex` subcommand exit before it does anything, `codex login status` included; the escape hatches are `--ignore-user-config` or `-c` overriding the key back to something legal
- [2026-08-28] `cargo test --lib` regenerates `docs/specs/ipc-manifest.*` and then fails the very test that wrote them, so any change to the command table costs two full 75s suite runs before you can see a green one
- [2026-08-28] `claude -p --output-format stream-json` reports the subagent tool as `Task` in its init `tools` list, but the model calls it `Agent` and `--allowedTools`/`--tools` only accept `Agent` — the one name the stream never prints
- [2026-08-28] `--tools ""` silently takes the subagent tool away too, so a system prompt that tells the agent to fan out has nothing to fan out with and no error says so; you find out from a run that quietly did everything single-handed
- [2026-08-28] the headless suite's `total()`-shaped helpers count `role: "card"` by *exclusion* ("everything but Waveform and Ruler"), so adding one chrome card to a screen breaks an unrelated Cmd+D assertion in a file you never opened — an allow-list of clip labels would have said what it meant
- [2026-08-28] a node captured from `app.snapshot()` and clicked a few `app.frames` later throws `StaleFrame`, but the error names the *click*, not the stale capture — every reusable handle in a walk has to be a `() =>` and nothing in the API hints at it
- [2026-08-28] sqlx 0.9's `SqlSafeStr` bound means a `format!`-composed query no longer type-checks, so sharing a SQL fragment between two statements has to be a `macro_rules!` returning a literal plus `concat!` — a `const &str` cannot be interpolated into a `format!` string and stay `&'static`
- [2026-08-28] a pixel test that picks its mid-animation frame by a coordinate band gets exactly one sample per ~500ms of a stretched 2.7s tween, so a 120px-wide band is a coin flip (`9, 163, 225…` on a real run) — band the whole interior of the travel, not a slice of it
- [2026-08-28] running any headless target while another agent is mid-rename in the same test binary fails at `mod` resolution, and the failure names *their* file, not yours — `cargo test --test headless <filter>` cannot be scoped below the binary
- [2026-08-27] gpui `Styled` has no `invisible()`/`visible()` at the pinned rev, so hiding an element mid-transition means `.opacity(0.)` — and every web-shaped instinct about visibility costs a compile round-trip to find out
- [2026-08-27] a gpui a11y node clipped out of its scroller reports a box you cannot read an offset from, so `until(node.bounds.x <= -60)` for "this level slid left" never fires — read the *arriving* side's positive coordinate instead, and bound it on both sides or the first frame (a full column-width away) matches too
- [2026-08-27] `app.screenshot()` in the pixel harness rasterizes at a moment that is not the `until` snapshot's, so a mid-animation shot chosen by predicate can land on the settled frame — pick the frame by a property that is monotonic across the whole tween, not by "both things are mounted"
- [2026-08-27] `with_motion()` leaves the *sidebar's own* opening slide running when the track rows land, so any pixel test that compares the column's box across later frames has to wait for `bounds.width >= SIDEBAR_WIDTH` first — nothing in the fixture builder hints that turning motion on makes the resting state arrive late
- [2026-08-27] `gpui/BUILD.md` spells the headless suite as `cargo test --test headless tab_chrome`, which from `gpui/` answers "no test target named `headless` in default-run packages" and then helpfully lists `headless` as available — the workspace has no default-run member, so every example in that file silently needs `-p gpui-agent`
- [2026-08-27] `cargo build -p luma-app` is not a check that the app compiles: `agent_node`/`agent_disabled` are no-ops returning `Self` without the `agent` feature and real wrappers with it, so a builder chain that type-errors in every test build finishes clean in the default one
- [2026-08-27] a bare `git checkout -- .` in this shared checkout wiped every other agent's uncommitted work along with the tree I was mid-edit in; verifying a change while three agents write the same files needs a `git worktree`, and standing one up costs a symlink for the gitignored `src-tauri/python` plus hand-stripping the other agents' half-finished hunks out of `dispatch/mod.rs` and `lib.rs` before it will even compile
- [2026-08-27] `load_verified_snapshot_for_connection`'s third argument is a bare `allow_expired: bool`, and the whole difference between "boot admits this session" and "boot cannot read this session" is which literal you pass — access tokens last an hour, so `false` fails on every launch that is not within an hour of a sign-in, and the error it returns ("Supabase access token has expired") reads like a real problem rather than a flag choice
- [2026-08-27] `git stash` in this checkout is a shared-tree hazard: it swallowed two other agents' uncommitted work along with mine, and `git stash pop` then refused because they had written again in the meantime — `git add -A` first makes the pop a real three-way merge, but the only reason nothing was lost is that I happened to back the tree up before trying
- [2026-08-27] `docs/specs/ipc-manifest.{json,md}` were already stale in HEAD (`list_scores_across_venues` was on the table but not in the file), so adding one command produces a diff carrying somebody else's regeneration; and the check needs *two* runs to go green — the first rewrite still fails, the second passes on the identical bytes
- [2026-08-27] `security find-generic-password -s "Claude Code-credentials" -w` without `-a $USER` returns whichever item the keychain finds first — on this machine a stale `root` entry that expired eight months ago, sitting next to the live one. The API just says "OAuth access token is invalid", so it reads as a broken endpoint rather than a wrong-row lookup
- [2026-08-27] `.agent_node()` is a no-op passthrough without the `pixel`/instrumented feature and an `Instrumented<T>` wrapper with it, so a helper typed `-> Div` compiles green under `cargo check` and fails only in the pixel build — a whole extra 4-minute round trip to learn the return type has to be `AnyElement`
- [2026-08-27] `gpui/crates/app/src/welcome.rs` is the venue picker *dialog*, not a welcome screen — the only full-screen "welcome" in the repo is the React `src/features/app/components/welcome-screen.tsx`, so "do it like welcome.rs" points at a modal and there is no full-screen-state precedent in the gpui app to copy
- [2026-08-27] the node protocol reports a flex-crushed control as a real node with a real box — the args strip's tint swatch came back as `{ width: 0 }` and its mode select as `width: 11` — so a geometry assertion reading only `x + width` cannot tell "laid out past the right edge" from "squeezed to nothing and unclickable"; the widths have to be sanity-checked against the control's own constant
- [2026-08-27] renaming a `pub fn` in `src-tauri` builds green in that workspace and leaves the whole `gpui` workspace uncompilable — `luma_lib` is a path dependency of `luma-app`, but nothing in a `src-tauri` check, clippy or test run looks at its out-of-workspace callers
- [2026-08-27] every `harness.exec` in a gpui-agent test shares one JS realm, so a `const gate` in the second script fails with `redeclaration of 'gate'` and no hint that the previous script is still in scope
- [2026-08-27] `.agent_node()` comes from the `Instrument` trait, not `AgentNode` (which only has `agent_disabled`/`agent_focused`) — importing the obvious one gives `no method named 'agent_node' found for struct 'gpui::Div'` with no trait suggestion
- [2026-08-27] a headless bin booting against a copy of the real library silently spends the *app's* single-use Supabase refresh token — the failure surfaces as `refresh_token_already_used` in an unrelated bin, and reproducing it costs a signed-out app; copying `state.db` into a scratch dir is not isolation when the credential inside it is global
- [2026-08-27] `auth_principal_proof` hashes the whole session JSON, so "corrupt the refresh token to simulate a dead one" is untestable — any edit to the stored bytes fails proof validation first; blackholing the network with `HTTPS_PROXY=http://127.0.0.1:1` is the only way to prove a boot path is offline
- [2026-08-27] `cargo build --bin X` after a `git stash pop` happily reuses the artifact built from the stashed tree — the binary I "verified" was the pre-change one, and nothing in the output says which source it came from
- [2026-08-27] `luma-ui` has two tiers (slab controls for planes, `float::*` for glass) but the split lives only in `float.rs`'s module prose — `luma_button` and `luma_checkbox` were `pub` and compose onto a dialog card with no warning, so the fixture picker grew a BUTTON_CLASS footer that type-checked fine; nothing but a reviewer's eye stops a plane control landing on glass
- [2026-08-27] every pixel test names its own env var for where the PNGs land (`LUMA_PICKER_SHOTS`, `LUMA_SHOTS`, `LUMA_COST_WINDOW`, and `track_editor_strip` hardcodes `$TMPDIR/luma-args-strip`) — there is no one knob, so re-capturing two dialogs to compare them means reading two `keep()`/`preserve()` helpers first
- [2026-08-27] the args strip overflows the harness's default 1200pt window — the `how many` and `tint` cells render past the right edge, and `app.screenshot({ node: strip() })` crops to the node box *clipped by the window*, so the shot looks like a complete strip while silently missing cells; only `.window(1800., 800.)` on the fixture reveals them
- [2026-08-27] `harness/README.md` documents the component / golden / graph-gauntlet captures but never mentions the track-editor pixel tests (`cargo test -p gpui-agent --features pixel --test app_pixel track_editor_strip`) or that their PNGs land in `$TMPDIR/luma-args-strip/` — the only way to find the capture path is reading the test's `keep()` helper
- [2026-08-27] gpui allows exactly one `on_hover` per element and `float::menu_row` already spends it on the row's own fade, so adding hover *state* to a menu row panics the pump thread mid-script ("calling on_hover more than once") and the test only reports `PumpGone` — wrap the row instead
- [2026-08-27] the overlay context list was written out twice in `keymap.rs` (once as `!a && !b && …` for the shell, once as `a || b || …` for Escape), so a new dialog is silently missing either its Escape or its ⌘B suppression until someone notices — folded into one `context::DIALOGS` array
- [2026-08-27] `stage_render`'s offscreen frames come back RGBA (what a PNG wants) while gpui's `RenderImage` reads its buffer as BGRA, and nothing at either signature says so — the picture just renders with red and blue swapped
- [2026-08-27] `luma-record`'s mp4 is not byte-reproducible run to run (the encoder's own nondeterminism), so "hash a frame before and after your change" cannot mean hashing the file — you have to decode a frame to rawvideo and compare that, and nothing says so
- [2026-08-27] selection expressions negate with `~`, not `!`; `!moving_spots` lexes into the error "Unexpected token And", which names a token that is nowhere in the input
- [2026-08-27] `ffmpeg_env`'s dev search is relative to the *binary's* directory, so a `cp target/perf/luma-record /tmp/before` used as an A/B baseline silently encodes with a different ffmpeg than the one in the tree — the two recordings then differ for a reason that has nothing to do with the change under test
- [2026-08-27] The bundled python env has no pytest and `unittest discover -s luma_exec/tests` dies with "Start directory is not importable"; the only invocation that works is `python3 -m unittest luma_exec.tests.test_track` from `src-tauri/python`, which nothing documents.
- [2026-08-27] `cargo check -p luma --lib` in `src-tauri` still compiles `gpui/crates/render` (the lib depends on it for `venue.render`), so a second agent's half-finished edit in a crate you were told not to touch blocks every build and test you can run — `-p`/`--lib` buys no isolation from that dependency edge
- [2026-08-27] the repo's python tests need `~/Library/Caches/com.luma.luma/python-env/bin/python3`; a system `python3` fails deep inside matplotlib's `pyexpat` import with a dyld symbol error that says nothing about the real problem, and nothing in AGENTS.md names the interpreter to use
- [2026-08-27] `luma_render` has four `submit_readback` call sites whose only difference is a bare positional `true`/`false` for `temporal` — I read the wrong one, built a whole mode on the belief that capture already ran the temporal pass, and only caught it by printf-probing the GPU path; a `Temporal(bool)`-shaped argument would have made the mistake impossible
- [2026-08-27] `cargo build` in `src-tauri` after editing `gpui/crates/render` sometimes reports only "Compiling luma" in the tail of its output while having rebuilt the dependency too, so `tail -3` is not evidence about what got rebuilt
- [2026-08-27] zsh does not word-split unquoted `$var`, so every `for spec in "a b"; do set -- $spec` loop copied from a bash snippet silently binds `$2` to nothing and ffmpeg reports it as "Invalid duration specification for ss: -i"
- [2026-08-27] ffmpeg's `-shortest` quietly drops ~7 s of audio off every recording when the video arrives down a slow pipe — the file muxes, plays, and reports two stream durations that differ, and nothing anywhere warns you
- [2026-08-27] `ffmpeg_env`'s dev search hardcoded `target/debug` and `target/release`, so anything built with the repo's own `perf` profile fell through to system PATH and rendered fine with the *wrong* ffmpeg
- [2026-08-27] `headless_host::boot` runs migrations against whatever `--config-dir` it is given, so "read the real library read-only" actually means copying `luma.db`, `-wal`, `-shm` *and* `state.db` first — and nothing lists that set
- [2026-08-27] `HostConfig::parse_args` hard-errors on an unknown flag, so any new bin with flags of its own has to pre-split argv into "mine" and "shared" before it can call the shared parser
- [2026-08-27] `ffmpeg_env::init` takes a `&tauri::AppHandle`, so every headless bin silently falls back to system-PATH ffmpeg — the bundled binary is right there in `src-tauri/ffmpeg-runtime/` and nothing says you didn't get it until a spawn fails
- [2026-08-27] video export exists on the unmerged `feat/video-export` branch and nothing in the tree mentions it, so `git log --diff-filter=D` for the "deleted" `commands/export.rs` returns nothing and you conclude it never existed
- [2026-08-27] the model-facing traceback in a headless transcript is truncated mid-frame, so the one line that names the failing host call is exactly the line you never get — reproducing the cell in a Rust test was faster than reading the log
- [2026-08-27] `luma.track` is reinstalled between cells but not by `apply()`, so a cell that applied and then rendered was told "track changed while this edit was open" by its own commit (fixed — apply advances the binding)
- [2026-08-27] the Python package has three test entry points (`test_track.py`, `test_venue.py`, `run_tests.py`) and no runner that knows about all three; nothing in AGENTS.md mentions the app venv at `~/Library/Caches/com.luma.luma/python-env/bin/python3` that they need
- [2026-08-27] biome ignores `scripts/`, so `bunx biome check --write scripts/headless/*.ts` prints "Checked 0 files" and exits 1 — the formatter you're told to run can't touch half the code you're asked to edit
- [2026-08-27] `claude --help` documents no `--max-turns`, yet the flag works — an agent capping a headless run has to guess that the SDK's option is still wired into the CLI
- [2026-08-27] AGENTS.md's build list never mentions `luma-mcp`, though `.mcp.json` points straight at `src-tauri/target/debug/luma-mcp` — you learn how to build it from an error string buried in `scripts/headless/mcp_smoke.ts`
- [2026-08-27] ugh, `.gitignore` ignores all of `/docs/*` with per-directory exceptions, so this very file was silently untracked and `git add` looked like it worked (fixed since, but the rule should be inverted)
- [2026-08-27] nothing tells another process that a document head moved, so the only way a second process learns it was behind is by losing a CAS it already did the work for
- [2026-08-27] `bin/luma-mcp` built services with a bare `Arc::new(boot(...))` and every `agent_turn_start` failed silently because the turn loop needs `into_shared()` (fixed since — `SharedServices` is a newtype now, so the bare form doesn't typecheck)
- [2026-08-27] lost an afternoon to the Python worker "hanging" — Bun hands children `RLIMIT_NOFILE = i64::MAX` and loky's fork path closes fds in a loop bounded by it (clamped in `worker.py` now, but anything else we spawn from Bun inherits it)
- [2026-08-27] `resources/` is found three different ways by three subsystems and every new binary reinvents the search, wrong on the first try — just give the crate one `resources_root()`
- [2026-08-27] burned time on a debounce that never fired: headless gpui virtualizes time, so `background_executor().timer()` hangs forever under `cargo test` while working fine in pixel mode
- [2026-08-27] spent a session on a "deaf" dialog before learning gpui computes focus from the rendered frame, so a stale handle still says `is_focused` and eats every key — test with `contains_focused`
- [2026-08-27] `git stash` is process-global, so concurrent agents in one checkout share a stack and another agent nearly popped my `stash@{0}` — use a worktree or a named branch
- [2026-08-27] one confusing sqlx compile error per new agent because rustup picks the toolchain from cwd, so `--manifest-path gpui/...` from the repo root silently runs 1.90 instead of the pinned 1.97.1
- [2026-08-27] every `chat` test collides with "database is locked" because the fixture db is keyed on the process id instead of the test name
- [2026-08-27] edited a migration file and broke every launch — sqlx checksums each version, and the only warning lived in a comment inside the migrations, not in AGENTS.md (fixed since)
- [2026-08-27] the ipc-manifest check writes the new files and *then* fails, so the first run after any command change is always red and the tree always dirty — and hand-written prose lives inside the generated file waiting to be clobbered
- [2026-08-27] ran a filtered cargo test and it nuked `schema.ts` down to the types those tests touched — had to run the whole suite just to get `bun run build` green again
